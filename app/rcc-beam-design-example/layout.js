export const metadata = {
  title: 'RCC Beam Design Example Step by Step | CivilCalc Pro',
  description:
    'RCC beam design example with load calculation, bending moment, effective depth, steel area and reinforcement design explanation.',
  alternates: {
    canonical: 'https://civilcalcpro.in/rcc-beam-design-example',
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-snippet': -1,
      'max-image-preview': 'large',
      'max-video-preview': -1,
    },
  },
}

export default function Layout({ children }) {
  return children
}
